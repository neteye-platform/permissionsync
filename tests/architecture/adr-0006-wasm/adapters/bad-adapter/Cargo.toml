[package]
name = "adapter-bad"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "permissionsync:bad-adapter"

[dependencies]
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }
