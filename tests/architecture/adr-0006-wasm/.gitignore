# Generated build artifacts - never commit.
target/
**/target/
*.wasm

# Generated cargo-component bindings (regenerated from canonical WIT on build).
**/bindings.rs

# Generated wit-bindgen-go bindings (regenerated from wit-build on build).
# Both the contract-facing packages (permissionsync/) and the wasi:cli deps the
# adapter-build world pulls in (wasi/) are regenerated output, never source.
adapters/go-adapter/permissionsync/
adapters/go-adapter/wasi/

# Regenerated from the canonical ../../wit/permissions.wit (one source of truth).
adapters/go-adapter/wit-build/permissions.wit

# Ephemeral test-only TLS materials (generated by tooling/gen_certs.sh).
generated/
